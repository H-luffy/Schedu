
# Excel模板使用指南

## 概述

本系统支持将Excel文件直接存放在项目中，网站在启动时自动扫描这些Excel文件并生成模板库，无需通过后台上传。

## Excel模板格式

Excel文件应遵循以下格式：

### 第一行：模板信息
- 第1列：模板名称（必填）
- 第2列：模板描述（可选）

### 第二行：颜色配置
- 第1列：背景色（默认：#ffffff）
- 第2列：表头背景色（默认：#f8fafc）
- 第3列：表头文字颜色（默认：#1e293b）
- 第4列：网格线颜色（默认：#e2e8f0）
- 第5列：单元格背景色（默认：#ffffff）
- 第6列：单元格文字颜色（默认：#64748b）
- 第7列：课程默认背景色（默认：#dbeafe）
- 第8列：课程文字颜色（默认：#1e3a8a）
- 第9列：冲突课程背景色（默认：#fee2e2）
- 第10列：冲突课程文字颜色（默认：#991b1b）

### 第三行：列标题
- 课程名称、星期、开始节次、结束节次、教室、教师、颜色等

### 后续行：课程数据
- 每行代表一个课程，包含课程名称、星期、开始节次、结束节次、教室、教师、颜色等信息

## 使用步骤

1. 创建Excel文件，按照上述格式填写模板信息和课程数据
2. 将Excel文件（.xlsx或.xls格式）放入项目的`excel-templates`目录
3. 启动或重启项目，系统会自动扫描并解析Excel文件
4. 在主页或模板页面，您将看到从Excel解析的模板

## 注意事项

1. Excel文件名不会影响模板名称，模板名称由Excel文件第一行第一列决定
2. 如果多个Excel文件生成的模板ID相同，后加载的会覆盖先加载的
3. Excel模板会与系统预设模板合并，如果模板ID相同，Excel模板优先级更高
4. 确保Excel文件格式正确，否则可能导致解析失败
5. 系统会在启动时扫描Excel文件，如果添加了新的Excel文件，需要重启项目才能生效

## 示例

以下是一个简单的Excel模板示例：

| 模板名称 | 模板描述 |
|---------|---------|
| #ffffff | #f8fafc | #1e293b | #e2e8f0 | #ffffff | #64748b | #dbeafe | #1e3a8a | #fee2e2 | #991b1b |
| 课程名称 | 星期 | 开始节次 | 结束节次 | 教室 | 教师 | 颜色 |
| 高等数学 | 周一 | 1 | 2 | 教学楼101 | 张老师 | #dbeafe |
| 大学英语 | 周二 | 3 | 4 | 教学楼202 | 李老师 | #d1fae5 |

## 故障排除

1. 如果Excel模板没有显示，请检查：
   - Excel文件是否放在`excel-templates`目录下
   - Excel文件格式是否正确
   - 是否重启了项目

2. 如果模板显示不正确，请检查：
   - Excel文件中的颜色值是否为有效的十六进制颜色代码
   - 模板名称是否填写
   - 列标题是否正确
